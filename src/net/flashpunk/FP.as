package net.flashpunk 
{
	import flash.display.Stage;
	
	/**
	 * Contains two global variables (unique Stage and Engine) and some global shortcuts.
	 * @author Copying
	 */
	public final class FP 
	{
		/**
		 * Global variable that points into the the engine.
		 */
		public static var engine:Engine;
		
		/**
		 * Global variable that points into the stage.
		 */
		public static var stage:Stage;
	}

}